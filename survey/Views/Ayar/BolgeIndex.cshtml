@model IEnumerable<survey.Models.Bolge>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="panel settings-list-panel">
    <div>
        <h4>BÖLGE LİSTESİ</h4>
    </div>
    <div class="panel-body">

        <table class="table">
            <tr>
                <td></td>
                <th>
                    Bölge Adı
                </th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <a class="fa fa-pencil" title="Ünvan düzenleme" href="@Url.Action("BolgeEdit", "Ayar", new { id = item.BolgeId })"></a>
                        <a class="fa fa-trash-o" title="Ünvan sil" href="@Url.Action("BolgeDelete", "Ayar", new { id = item.BolgeId})"></a>
                    </td>

                    <td>
                        @item.BolgeAdi
                    </td>

                </tr>
            }

        </table>
        <div class="col-lg-9"></div>
        <div class="col-lg-3">
                <a style="background-color:green; color:white" class="btn btn-default" href="@Url.Action("Index", "Ayar")">Geri</a>
                <a style="background-color:blue; color:white" class="btn btn-default" href="@Url.Action("BolgeCreate", "Ayar")">Yeni</a>
        </div>

    </div>
</div>
